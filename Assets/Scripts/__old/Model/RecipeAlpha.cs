using UnityEngine;
using System.Collections;

public class RecipeAlpha : MonoBehaviour {

    //public Dish[] dishes;

	// Use this for initialization
	void Start () {
        /*
        if (dishes.Length < 1)
        {
            Debug.Log("No dishes in recipe!");
        }
        else
        {
            foreach (Dish dish in dishes)
            {
                Debug.Log(dish.title);

            }

        }
         */
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
